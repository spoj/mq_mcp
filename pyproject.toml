[project]
name = "mq-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.16.1",
    "mcp[cli]>=1.9.1",
    "httpx",
    "tenacity>=9.1.2",
]

[project.scripts]
mq-mcp = "server:main"
